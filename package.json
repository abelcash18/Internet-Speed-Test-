{
  "name": "internet-speed-test",
  "version": "1.0.0",
  "description": "Modern performant PWA internet speed test",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js || python -m http.server 8000",
    "build": "terser script.js -o script.min.js --compress --mangle && terser sw.js -o sw.min.js --compress --mangle && echo 'Build complete: script.min.js, sw.min.js'"
  },
  "keywords": [
    "speed",
    "test",
    "internet",
    "pwa",
    "performance"
  ],
  "author": "BLACKBOXAI",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "terser": "^5.46.2"
  }
}
